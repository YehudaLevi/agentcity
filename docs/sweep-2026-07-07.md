# Incumbent Sweep — 2026-07-07 (Gate 1)

> Concept evaluated: "AI-agent coding activity builds a persistent pixel-art
> city" (events → buildings/floors/districts by work taxonomy, decay, agent-
> authored blueprints, solo→team). Verdict: **PARTIALLY OCCUPIED — the
> specific combo is OPEN.** Gate 1 passed with conditions (see README).

## Ranked closest incumbents

1. **Pixel Agents** — github.com/pablodelucca/pixel-agents · ~8.5k★, 73,731
   VS Code installs, Fast Company/Inc. coverage, fork ecosystem (pixtuoid
   347★, claude-office 453★, Star-Office-UI ~4.8k★). Closest on INPUT axis:
   reads Claude Code tool-call transcripts live. **Ephemeral** — state resets
   per session; only furniture layout persists. No taxonomy, no history, no
   agent-authored art. A live status board, not a growing artifact. Its 73k
   installs = our strongest demand signal.

2. **Git City (srizzon)** — github.com/srizzon/git-city · thegitcity.com ·
   5,705★, 281 forks, active (pushed 2026-07-02), PH 118 upvotes,
   Stripe-monetized cosmetics. Closest on OUTPUT axis: persistent multiplayer
   pixel city — but from GitHub profile aggregates (building = user, not
   repo), human commits not agent work, no taxonomy, no decay, deterministic
   art. Proves the visual genre resonates + monetizes. **Must be named in
   any positioning.**

3. **AI Town (alexcloudstar)** — Claude chat export → one-off pixel town.
   Near-zero traction, no continuous sync, volume-only, no LLM in art.
   The link a commenter would post; preempted in one sentence.

4. **AgentPet** — 267★, active weekly. Agent-fed persistent progression
   (tokens→XP→evolutions, streaks, leaderboard, agent-agnostic). Pet with a
   scalar, not a place. No spatial/taxonomy/authored-art.

5. **Tamagotchi cluster** — claude-code-tamagotchi (~430★, decay),
   Codachi (40+ tool-call categories, local-first), Tamamon (gacha + habitat,
   PH days ago), openpets, Petdex (3.2k★). Every mechanic proven in
   miniature; all pet-scoped.

6. **GitHub-city clone wave** — GithubCity 1,289★ (abandoned), GitCity,
   DevCity (language→material mapping — nearest to "style = work mix"),
   Git City: Pulse (335 installs; glow/fade-on-inactivity presence). All
   human-commit, mostly stateless.

7. **CodeCity lineage** — CodeCity (2008), CodeCharta (~450★ active),
   JSCity 1.4k★ (dead). Codebase STRUCTURE as city, one-off renders.
   Academic citations for the structure-vs-activity gap: arXiv 2109.14217,
   2408.08141.

8. **Gource (13.2k★) / gh-skyline / git-wrapped** — replay/export of git
   history; never a maintained artifact.

Honorable mentions: gitgarden (wilts with neglect), cli-pet, Wakawaka,
Git Quest, Agent-town (210★ — roadmap mentions a "town map"; the fork most
likely to drift toward us).

## Axis table

| Axis | Proven by | Gap |
|---|---|---|
| Agent telemetry → pixel viz | Pixel Agents (73k installs) | ephemeral |
| Persistent pixel city from dev activity | Git City (5.7k★) | human GitHub stats, no taxonomy |
| Agent-fed persistent progression | AgentPet | pet, not spatial |
| Decay + tool-call taxonomy | tamagotchi cluster | pet-scale |
| AI-history → pixel town | AI Town | one-off, ~zero traction |
| **Agent-authored architecture (constrained blueprints)** | **nobody** | explicit negative finding |

## Positioning sentence (mandatory in any launch material)

"Git City renders what GitHub already knows; AI Town renders what you
already chatted. This is a save-file your agent's actual work grows, floor
by floor — and the agent draws its own buildings."

## Risks to monitor (re-verify changelogs immediately before build)

- Git City adding agent-telemetry input
- pixel-agents adding persistence
- Agent-town's "town map" roadmap item
- Tamamon/AgentPet pivoting pet → town ("decorate habitat" is one step away)

## Caveats

Space moves fast (3 of the top items are <4 months old). HN point counts
partially unconfirmed (Algolia rate limits). Canonical pixel-agents star
count: 8.5k.
